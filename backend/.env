# WeatherPro Backend (.env)
# Note: Railway 배포에선 대시보드 환경변수가 적용됩니다. 이 파일은 로컬/개발용입니다.

NODE_ENV=production

# CORS: 프론트 실제 도메인
CORS_ORIGIN=https://weather-production-3d13.up.railway.app
CORS_CREDENTIALS=true

# 보안 키(강력한 임의 문자열로 교체)
JWT_SECRET=REPLACE_WITH_STRONG_RANDOM_SECRET

# (옵션) 현재 코드에선 미사용. 필요 시 채워 사용하세요.
# DATABASE_URL=postgresql://username:password@localhost:5432/weatherpro_db
# REDIS_URL=redis://localhost:6379
# OPENWEATHER_API_KEY=your_openweather_api_key_here

# 로깅/레이트리밋(직접 참조는 적음)
LOG_LEVEL=info
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100
